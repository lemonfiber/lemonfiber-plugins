# Calls the reusable workflows defined once in lemonfiber/spec (Q-R56).
name: ci
on: [pull_request]
permissions:
  contents: read

jobs:
  # `pull-requests: write` is what lets the gate close a pull request whose
  # citation is missing or names an identifier that does not exist, rather than
  # only going red at it. A reusable workflow's permissions block is a request,
  # and a caller granting less than it asks for fails the whole run before any
  # job starts — every required context silent, and nothing to report against.
  # The workflow-level `contents: read` is the default for a job that declares
  # nothing, and a job-level block replaces that default outright, so
  # `contents: read` is restated here rather than inherited.
  spec-check:
    permissions:
      contents: read
      pull-requests: write
    uses: lemonfiber/spec/.github/workflows/spec-check.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  hygiene:
    uses: lemonfiber/spec/.github/workflows/hygiene.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  security:
    uses: lemonfiber/spec/.github/workflows/security.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  dco:
    uses: lemonfiber/spec/.github/workflows/dco.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  attribution:
    uses: lemonfiber/spec/.github/workflows/attribution.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  commitlint:
    uses: lemonfiber/spec/.github/workflows/commitlint.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
  labeler:
    permissions:
      contents: read
      pull-requests: write
    uses: lemonfiber/spec/.github/workflows/labeler.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
